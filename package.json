{
  "name": "unifed-server-auth",
  "version": "1.0.0",
  "private": true,
  "description": "Endpoint de autenticação server-side (validação fora do cliente), com motor de dispersão SHA-256 e comparação em tempo constante.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "hash-password": "node generate-hash.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
